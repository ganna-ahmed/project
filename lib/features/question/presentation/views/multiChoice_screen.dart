import 'package:flutter/material.dart';
class MultiChoiceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Multi Choice Screen")),
      body: Center(child: Text("This is the Multi Choice screen")),
    );
  }
}